# MP3-Tag-Reader
Developed a C-based application to read and modify metadata tags of MP3 files using the ID3v2.3 format. The program extracts fields such as Title, Artist, Album, Year, and Genre from the file header and allows selective editing. The updated metadata is written to a new file while preserving the original audio data integrity.
